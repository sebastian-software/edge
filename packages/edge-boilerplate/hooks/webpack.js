/* eslint-disable import/unambiguous, import/no-commonjs */
module.exports = function webpack(config, { isServer, isClient, isProduction, isDevelopment }) {
  // Extend and manipulate the Webpack configuration here.
  return config
}
