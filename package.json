{
	"private": true,
	"devDependencies": {
		"lerna": "^2.4.0"
	},
	"scripts": {
		"update": "lerna exec ncu -- -ua && lerna bootstrap",
		"prepare": "npm run build",
		"test": "lerna run test",
		"build": "lerna run build"
	},
	"workspaces": [
		"packages/*"
	]
}
